spring:
  application:
    name: Day68
  datasource:
    url: jdbc:mysql://localhost:3306/yonsai_db
    username: root
    password: ${DB_PW}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

